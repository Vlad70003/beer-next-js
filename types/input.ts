export interface inputState {
    border?: string,
    width?: string,
    type?: string,
    disabled?: boolean,
    checked?: boolean,
    placeholder?: string,
    margin?: string,
    maxLength?: any,
}