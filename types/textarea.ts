export interface textareaState {
    border?: string,
    width?: string,
    type?: string,
    disabled?: boolean,
    checked?: boolean,
    placeholder?: string,
    margin?: string,
    maxLength?: any,
    resize?: boolean,
    rows?: number,
}